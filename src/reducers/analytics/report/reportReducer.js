import { ACTION_TYPE } from '../../../constants/actionTypes';

const initialState = {
    
};
const AnalyticsReportReducer = (state = initialState, action) => {
  switch (action.type) {
    
    default: {
      return state;
    }
  }
};
export default AnalyticsReportReducer;
